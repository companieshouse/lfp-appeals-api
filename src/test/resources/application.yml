server:
  port: 9000

spring:
  data:
    mongodb:
      uri: "mongodb://localhost/test"
  main:
    lazy-initialization: true

chips:
  enabled: true
  restservice:
    url: "http://restservice/appeals"

companyNumber:
  prefixes: "SC,NI,OC,SO,R,AP"

